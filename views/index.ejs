<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Docker Site</title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <main>
      <article>
        <strong>hello world ^_^</strong>

        <h1><%=title %> в EJS</h1>

        <% if(emailsVisible) {%>
          <h3>Это нода на гитхабе!!!</h3>
          <ul>
            <% for(var i=0; i<emails.length;i++) {%>
              <li><%=emails[i] %></li>
              <%} %>
            </ul>
            <% }else {%>
              <h3>Электронный адрес отсутствует</h3>
              <%} %>
              <p>Excellent <%=phone %></p>
      </article>
      <aside>
        <ul>
          <a href='/'<li>one </li></a>
          <a href='/lol'<li>Two </li></a>
          <a href='/super'<li>Three </li></a>
        </ul>
      </aside>
    </main>
  </body>
</html>
